# nacos配置
server:
  port: 3377

spring:
  application:
    name: nacos-config-client
  cloud:
    nacos:
      discovery:
        server-addr: 192.168.43.197:1111 #Nacos服务注册中心地址
      config:
        server-addr: 192.168.43.197:1111 #Nacos作为配置中心地址
        file-extension: yml #指定yaml格式的配置
        group: PROD_GROUP
#        namespace: b7918a68-f415-4d16-a19a-c46e22e3a69b


  # ${spring.application.name}-${spring.profile.active}.${spring.cloud.nacos.config.file-extension}
  # nacos-config-client-dev.yaml






